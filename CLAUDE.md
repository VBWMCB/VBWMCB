# CLAUDE.md

Guidance for AI assistants (Claude Code and others) working in this repository.

## Current state

This repository is **empty** — it has no source code, build configuration, or
prior commit history yet. This file is a starting point and should be expanded
as the project takes shape. When real code lands, replace the placeholder
sections below with concrete, verified details (commands, paths, conventions).

> Maintenance note: Keep this file accurate. If you add a language, framework,
> build tool, or test runner, document the exact commands here and remove any
> guidance that no longer applies. Do not document conventions that aren't
> actually used in the codebase.

## Repository

- Remote: `VBWMCB/VBWMCB`
- Primary working directory: repository root

## Codebase structure

_To be documented._ There are currently no files to describe. As the project
grows, record the top-level layout here (e.g. `src/`, `tests/`, `docs/`) and
note where the entry points, configuration, and shared utilities live.

## Development workflow

_To be documented._ Once tooling exists, capture the canonical commands, for
example:

- Install dependencies: _TBD_
- Build: _TBD_
- Run locally: _TBD_
- Run tests: _TBD_
- Lint / format: _TBD_

Prefer documenting the single command a contributor should run for each task,
and verify each command works before adding it.

## Conventions

_To be documented._ Capture code style, naming, directory conventions, commit
message format, and any project-specific patterns once they are established.

## Git workflow

- Develop on a dedicated feature branch; do not commit directly to the default
  branch.
- Write clear, descriptive commit messages.
- Push with `git push -u origin <branch-name>`.
- Do not open a pull request unless explicitly asked.
