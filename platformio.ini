; ============================================================================
;  ESP32-2432S028R  "Cheap Yellow Display" (CYD)
;  YouTube channel statistics dashboard
;
;  Board : ESP32-WROOM-32  +  2.8" ILI9341 TFT (240x320)  +  XPT2046 touch
;  Flash : `pio run -t upload`   (see README.md)
; ============================================================================

[env:esp32-2432s028r]
platform  = espressif32
board     = esp32dev
framework = arduino

monitor_speed   = 115200
upload_speed    = 921600
monitor_filters = esp32_exception_decoder

lib_deps =
    bodmer/TFT_eSPI @ ^2.5.43
    paulstoffregen/XPT2046_Touchscreen @ ^1.4
    bblanchon/ArduinoJson @ ^7.2.0

; ---------------------------------------------------------------------------
;  TFT_eSPI is configured entirely through build flags here, so you never
;  have to edit files inside the library (User_Setup.h). These pins are the
;  fixed wiring of the ESP32-2432S028R display bus (HSPI).
; ---------------------------------------------------------------------------
build_flags =
    -DUSER_SETUP_LOADED=1
    -DILI9341_2_DRIVER=1        ; CYD-friendly ILI9341 variant
    -DTFT_WIDTH=240
    -DTFT_HEIGHT=320
    -DTFT_MISO=12
    -DTFT_MOSI=13
    -DTFT_SCLK=14
    -DTFT_CS=15
    -DTFT_DC=2
    -DTFT_RST=-1               ; tied to the ESP32 reset line
    -DTFT_BL=21                ; backlight
    -DTFT_BACKLIGHT_ON=HIGH
    -DSPI_FREQUENCY=40000000
    -DSPI_READ_FREQUENCY=20000000
    ; fonts
    -DLOAD_GLCD=1
    -DLOAD_FONT2=1
    -DLOAD_FONT4=1
    -DLOAD_FONT6=1
    -DLOAD_FONT7=1
    -DLOAD_FONT8=1
    -DLOAD_GFXFF=1
    -DSMOOTH_FONT=1
